# Optional: copy to Client/.env — Vite only exposes vars prefixed with VITE_

# Google Sign-In button (must match Server GOOGLE_CLIENT_ID if you use Google login)
# VITE_GOOGLE_CLIENT_ID=

# If the API is not proxied from the Vite dev server, set the full URL:
# VITE_API_URL=http://localhost:3000
